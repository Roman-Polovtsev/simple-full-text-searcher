public class Test {

        /*

    create table products(id integer, name text)
    create table categories(id integer, name text)

    create table product_categories(product_id integer, category_id integer,
        foreign key (product_id) references products.id,
        foreign key (product_id) references products.id);

    запрос на нахождение имен категорий, у которых как минимум 3 продукта



     */

    /*
    class Category {
    String parentId;
    String id;
    String name;
}

class InvertedCategory{
    List<String> childId;
    String id;
    String name;
}

// categoryId -> ..
//            -> .. -> rootCategory


Set<String> getAllParentCategories(List<Category> categories, String categoryId) {
     */

}
